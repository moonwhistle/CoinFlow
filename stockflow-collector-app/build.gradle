plugins {
    id 'org.springframework.boot'
}

dependencies {
    implementation project(':stockflow-core')
    implementation project(':stockflow-common')
    implementation project(':stockflow-infra-redis')
}

bootJar { enabled = true }
jar { enabled = false }
